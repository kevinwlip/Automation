from selenium.webdriver.common.by import By
import logging

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

class ServersLocators(object):
    """ Element locators for UI Servers """

class ApplicationsLocators(object):
    """ Element locators for UI Applications """

class NetworkLocators(object):
    """ Element locators for UI Network """

class ProfilesLocators(object):
    """ Element locators for UI Profiles """

